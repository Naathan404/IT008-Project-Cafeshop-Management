<Page x:Class="CoffeeShop.View.Staff.Staff_History"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CoffeeShop.View.Staff"
      mc:Ignorable="d" 
      d:DesignHeight="610" d:DesignWidth="1110"
      Title="Staff_History">

    <Grid Background="Wheat">
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"></RowDefinition>
            <RowDefinition Height="20*"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0"
                    Orientation="Vertical">
            
        </StackPanel>
        <Border Grid.Row="1" BorderBrush="#FF340D05"
                BorderThickness="1"
                Margin="100 0 100 20">
            
            <DataGrid x:Name="dgOrdersHistory"
                    ColumnHeaderStyle="{StaticResource dgTemplate}"
                    AutoGenerateColumns="False" CanUserAddRows="False"
                    CanUserResizeColumns="False" CanUserReorderColumns="False"
                    HorizontalAlignment="Stretch"
                    GridLinesVisibility="None"
                    IsReadOnly="true">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã hóa đơn" Width="3*" Binding="{Binding OrderID}"></DataGridTextColumn>
                    <DataGridTextColumn Header="Tên khách hàng" Width="4*" Binding="{Binding CustomerName}"></DataGridTextColumn>
                    <DataGridTextColumn Header="Tên nhân viên" Width="4*" Binding="{Binding EmployeeName}"></DataGridTextColumn>
                    <DataGridTextColumn Header="Ngày tạo" Width="5*" Binding="{Binding OrderDate}"></DataGridTextColumn>
                    <DataGridTextColumn Header="Tổng tiền" Width="5*" Binding="{Binding Total}"></DataGridTextColumn>
                    <DataGridTextColumn Header="Hình thức thanh toán" Width="5*" Binding="{Binding PaymentMethod}"></DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>
