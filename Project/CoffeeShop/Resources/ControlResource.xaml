<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- TEMPLATER BUTTON dùng cho nhanh 2-->
    <ControlTemplate x:Key="BrownButtonTemplate_1" TargetType="Button">
        <Border Background="{TemplateBinding Background}"
            BorderBrush="#FF340D05"
            BorderThickness="1"
            CornerRadius="10"
            Height="{TemplateBinding Height}">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                Content="{TemplateBinding Content}"/>
        </Border>
    </ControlTemplate>
    <Style x:Key="BrownButtonStyle_1" TargetType="Button">
        <Setter Property="Template" Value="{StaticResource BrownButtonTemplate_1}"/>
        <Setter Property="Height" Value="50"/>
        <Setter Property="Width" Value="50"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="Background" Value="#C7A57A"/>
        <Setter Property="Foreground" Value="#F9F6F0"/>
        <Setter Property="FontFamily" Value="Cascadia Code"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    
    <!-- TOOLTIP super cute vip pro sến súa dùng được cho mọi loại control - Kt. Nguyen Nguyen-->
    <Style x:Key="CustomTooltip" TargetType="ToolTip">
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="#E0CEB5"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="10"/>
        <Setter Property="Opacity" Value="0.95"/>
        <Setter Property="FontFamily" Value="Cascadia Code"/>
        <Setter Property="Foreground" Value="#FF340D05"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="Placement" Value="Mouse"/>
        <Setter Property="HasDropShadow" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToolTip">
                    <Border CornerRadius="8"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter />

                        <Border.Effect>
                            <DropShadowEffect ShadowDepth="2" BlurRadius="5" Opacity="0.3"/>
                        </Border.Effect>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- BOOLEAN to VISIBILITY-->
    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

    <!-- CIRCLE LOADING INDICATOR -->
    <Style x:Key="CircularProgress" TargetType="ProgressBar">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Viewbox>
                        <Canvas Width="40" Height="40">
                            <Path Fill="{TemplateBinding Foreground}" 
                              Data="M20,0 A20,20 0 0 1 40,20 L35,20 A15,15 0 0 0 20,5 Z">
                                <Path.RenderTransform>
                                    <RotateTransform Angle="0" CenterX="20" CenterY="20"/>
                                </Path.RenderTransform>
                                <Path.Triggers>
                                    <EventTrigger RoutedEvent="Loaded">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" 
                                                             From="0" To="360" Duration="0:0:1" RepeatBehavior="Forever"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Path.Triggers>
                            </Path>
                            <Path Stroke="LightGray" StrokeThickness="5" 
                              Data="M20,5 A15,15 0 1 1 5,20" Opacity="0.3"/>
                        </Canvas>
                    </Viewbox>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- Color progress bar -->
    <DataTemplate x:Key="ColorProgressBar">
        <Grid Margin="10">
            <Border CornerRadius="5" ClipToBounds="True">
                <ProgressBar 
                    Value="{Binding UsagePercentage, Mode=OneWay}" 
                    Height="10" 
                    Minimum="0" Maximum="100"
                    Background="#E0E0E0">
                    <ProgressBar.Style>
                        <Style TargetType="ProgressBar" BasedOn="{StaticResource MaterialDesignLinearProgressBar}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding PerformanceLevel}" Value="Tệ">
                                    <Setter Property="Foreground" Value="#e74c3c"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding PerformanceLevel}" Value="Bình thường">
                                    <Setter Property="Foreground" Value="#f1c40f"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding PerformanceLevel}" Value="Tốt">
                                    <Setter Property="Foreground" Value="#2ecc71"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ProgressBar.Style>
                </ProgressBar>
            </Border>

            <TextBlock 
                HorizontalAlignment="Center" VerticalAlignment="Center"
                FontFamily="Cascadia Code" FontSize="10" FontWeight="Bold" Foreground="#2c3e50">
                <TextBlock.Text>
                    <MultiBinding StringFormat="{}{0}/{1}">
                        <Binding Path="UsedCount" />
                        <Binding Path="UseLimit" />
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
        </Grid>
    </DataTemplate>
</ResourceDictionary>